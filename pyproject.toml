[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aipc"
version = "0.1.0"
description = "AI Powered Policy Compliance System"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]